void Reverse(char * string)
{
	if(!string)
		return;

	char* end = string;
	while(*tail)
	{
		tail++;
	}
	tail--;

	for( ; string < tail; string++, tail--)
	{
		char s = *string;
		char t = *tail;
		*string = t;
		*tail = s;
	}
}